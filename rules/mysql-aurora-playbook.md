---
paths:
  - "**/*.sql"
  - "**/schema/**"
  - "**/migrations/**"
  - "**/migrate/**"
---

# Rule of MySQL Aurora playbook

When writing MySQL queries, designing Aurora schemas, optimizing indexes, troubleshooting slow queries, fixing deadlocks, planning schema migrations, or tuning Aurora connection pools and failover, invoke the `my-claude-code:mysql-aurora-playbook` skill via the Skill tool before proceeding.
